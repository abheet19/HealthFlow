{
  "name": "healthflow-demo-recorder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Records the HealthFlow hero demo GIF against the deployed app. Not part of the app build.",
  "scripts": {
    "record": "node record-demo.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
